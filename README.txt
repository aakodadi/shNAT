shNAT (shun the NAT = éviter, fuir, esquiver le NAT) :
deux applications qui travaillent ensemble,
pour rendre un serveur qui est dans un réseau local
(dans un LAN, protégé avec du NAT),
accessible depuis l'exterieur sans faire une redirection des ports